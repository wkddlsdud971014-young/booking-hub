# 명령어 - 터미널에 그대로 복사해서 붙여 넣으세요

> **`_env.yaml` 의 `kind` 가 `web` 이면 이 문서 아래쪽 "웹 실습" 절로 바로 갑니다.**
> 아래 0-8 번은 `kind: python` 일 때입니다.

> VS Code 에서 터미널 여는 법: 메뉴 `Terminal` > `New Terminal`

> 순서대로 합니다. 건너뛰지 않습니다. 지난 시간에 했더라도 처음부터 다시 합니다.
> 각 단계에 **이렇게 보이면 성공** 이 있습니다. 그 줄이 안 보이면 다음 단계로 가지 않습니다.


## 0. 폴더를 제자리에 둡니다

실습 폴더는 **`dev` 폴더 안**에 둡니다. 바탕화면이나 다운로드 폴더에 두면
경로에 한글이나 빈칸이 섞여 설치가 중간에 멈춥니다.

**Windows** - `C` 드라이브에 `dev` 폴더 하나를 만듭니다. 최종 위치는 `C:\dev\booking-hub` 입니다.

**Mac** - 터미널을 열고 이 한 줄이면 만들어집니다.

```
mkdir -p ~/dev && open ~/dev
```

Finder 로 찾아가려면 `매킨토시 HD > 사용자 > 내 계정 이름` 입니다.
왼쪽 목록에 매킨토시 HD 가 안 보이면 위 명령을 쓰는 편이 빠릅니다.

### 압축을 풉니다

받은 압축 파일을 `dev` 폴더로 **옮긴 다음** 거기서 풉니다.
다운로드 폴더에서 풀고 나중에 옮기면 경로가 두 군데로 갈립니다.

`dev` 폴더 안에 `booking-hub` 라는 폴더가 생깁니다.

### VS Code 로 엽니다

메뉴 `File` > `Open Folder` 로 그 `booking-hub` 를 엽니다.
폴더 안의 파일이 아니라 **폴더 자체**를 엽니다.

**이렇게 보이면 성공**

```
왼쪽 목록에 ENTRY 파일이 보입니다.
창 위쪽 제목이 booking-hub 입니다.
```

**안 되면**

- 파일 하나만 열렸다 -> 폴더를 안 연 것입니다. `File > Open Folder` 를 다시 합니다.
- 왼쪽 목록에 `booking-hub` 가 또 있다 -> 폴더가 두 겹입니다. 안쪽 것을 엽니다.
- 폴더 이름이 `booking-hub 2`, `booking-hub 3` -> 압축을 여러 번 풀었습니다.
  나머지를 지우고 하나만 남깁니다.
- 점으로 시작하는 파일(`.env.example` 등)이 안 보인다 -> 탐색기와 Finder 는 숨깁니다.
  **VS Code 왼쪽 목록에는 보입니다.** 거기서 확인합니다.

### 한글이 들어가면 안 되는 곳 셋

경로에 한글이 있으면 설치가 엉뚱한 오류로 실패합니다. 아래 셋은 나중에 고치기 어렵습니다.

| 어디 | 확인 |
|---|---|
| 폴더 이름 | `dev`, `booking-hub` - 영문 그대로 씁니다 |
| Windows 사용자 계정 이름 | `C:\Users\` 안의 내 폴더 이름이 한글이면 강사를 부릅니다 |
| 컴퓨터 이름 | 한글이면 강사를 부릅니다 |

## 1. 파이썬이 어느 것인지 봅니다


**Windows**

```
python --version
```

**Mac**

```
python3 --version
```


**이렇게 보이면 성공**

```
Python 3.10.0 처럼 3.10 이상의 숫자가 나옵니다.
```

**안 되면**

- 3.9 로 시작하면 -> 그대로 진행하되 3번을 절대 건너뛰지 마세요.
- (건너뛰면 설치가 몇 분 돌다가 빨간 글씨로 죽습니다.)
- command not found -> 파이썬이 없습니다. 강사에게 알려주세요.

## 2. 자리를 맞춥니다

터미널 맨 앞에 booking-hub 가 보여야 합니다.
안 보이면 아래 한 줄을 붙여 넣습니다.

  cd booking-hub

**이렇게 보이면 성공**

```
터미널 줄 맨 앞에 booking-hub 라는 글자가 있습니다.
```

**안 되면**

- no such file or directory -> 다른 자리입니다. VS Code 를 닫고 0번부터 다시 합니다.
- .venv 는 폴더마다 따로 만들어집니다.
- 다른 폴더에서 만든 것은 여기서 쓰이지 않습니다.

## 3. 상자를 만들고 pip 를 새것으로 바꿉니다


**Windows**

python -m venv .venv
  .\.venv\Scripts\python.exe -m pip install --upgrade pip

**Mac**

python3 -m venv .venv
  ./.venv/bin/python -m pip install --upgrade pip

**이렇게 보이면 성공**

```
Successfully installed pip-26.2.1
    (숫자는 달라도 됩니다. Successfully 라는 글자가 핵심입니다.)
```

**안 되면**

- 아무 반응이 없다 -> 1번의 파이썬 버전을 다시 봅니다.

## 4. 필요한 것을 설치합니다


**Windows**

.\.venv\Scripts\python.exe -m pip install "gradio<6" "huggingface_hub<0.26" google-generativeai python-dotenv

**Mac**

./.venv/bin/python -m pip install "gradio<6" "huggingface_hub<0.26" google-generativeai python-dotenv

  2 - 3 분 걸립니다. 글자가 계속 올라가는 것은 정상입니다.

**이렇게 보이면 성공**

```
Successfully installed ... gradio-5.14.0 ... google-generativeai-0.8.6 ...
```

**안 되면**

- Failed building wheel for cryptography -> 3번을 건너뛰었습니다. 3번을 하고 4번을 다시 합니다.

## 5. 설치가 됐는지 확인합니다


**Windows**

.\.venv\Scripts\python.exe -c "import gradio, google.generativeai, dotenv; print('설치 OK  gradio', gradio.__version__)"

**Mac**

./.venv/bin/python -c "import gradio, google.generativeai, dotenv; print('설치 OK  gradio', gradio.__version__)"

**이렇게 보이면 성공**

```
설치 OK  gradio 5.14.0

  같이 뜨는 이 줄은 정상입니다. 에러가 아닙니다.
    https://github.com/google-gemini/deprecated-generative-ai-python/...
```

**안 되면**

- ModuleNotFoundError -> 4번이 끝까지 안 갔습니다. 4번을 다시 합니다.

## 6. 열쇠를 넣습니다

폴더 안에 .env 파일이 이미 있습니다. 새로 만들거나 이름을 바꾸지 않습니다.
그 파일을 열고 자기 키를 넣습니다.

  GEMINI_API_KEY=여기에_내_API키

= 앞뒤에 빈칸을 넣지 않습니다. 따옴표도 넣지 않습니다.
키는 각자 것을 씁니다.

## 7. 열쇠가 들어갔는지, 모델이 대답하는지 확인합니다


**Windows**

.\.venv\Scripts\python.exe check.py

**Mac**

./.venv/bin/python check.py

**이렇게 보이면 성공**

```
키 OK  길이 39
    모델 OK  gemini-3.5-flash-lite  응답: 청색
```

**안 되면**

- 키 없음 -> .env 라는 이름이 맞는지 봅니다. .env.example 그대로면 안 됩니다.
- API key not valid -> 키를 다시 복사해 넣습니다. 앞뒤 빈칸을 지웁니다.
- 429 -> 1분 안에 15번을 넘겼습니다. 한도는 모델마다 따로 있습니다.
- 화면에 적힌 초만큼 기다렸다 다시 하면 됩니다.
- VS Code 의 Continue 도 같은 모델을 쓰면 서로 잡아먹습니다.
- 자주 나면 Continue 의 모델을 다른 것으로 바꾸세요.

## 8. 봇을 켭니다


**Windows**

.\.venv\Scripts\python.exe app.py

**Mac**

./.venv/bin/python app.py

**이렇게 보이면 성공**

```
Running on local URL:  http://127.0.0.1:7860
    그 주소를 브라우저에서 엽니다.

  끄는 것은 Ctrl + C 입니다.
  settings.py 를 고쳤으면 껐다가 다시 켜야 화면이 바뀝니다.
```

---

0 - 7 번은 처음 한 번만 합니다. 8 번이 봇을 켭니다.

 처음 값으로 되돌리려면
   **Windows**  .\.venv\Scripts\python.exe reset.py
   **Mac**      ./.venv/bin/python reset.py

---

# 웹 실습 (`kind: web`)

가상환경은 없습니다. 아래 순서만 합니다.

`package.json` 이 폴더에 있으면 **W1 다음에 W1-B(설치)** 를 합니다.
없으면 설치할 것이 없으니 W1 에서 바로 W2 로 갑니다.

## W0. 폴더를 제자리에 둡니다

**위 0 번 절과 같습니다.** `dev` 폴더를 만들고, 받은 압축을 그 안으로 옮겨서 풀고,
VS Code 메뉴 `File > Open Folder` 로 그 폴더 자체를 엽니다.

한글이 들어가면 안 되는 곳 셋(폴더 이름 / Windows 사용자 계정 이름 / 컴퓨터 이름)도 같습니다.

**이렇게 보이면 성공**
```
왼쪽 목록에 index.html 과 _env.yaml 이 보입니다.
창 위쪽 제목이 booking-hub 입니다.
```

## W1. Node 가 있는지 봅니다

```
node -v
```

**이렇게 보이면 성공**
```
_env.yaml 의 node_min 이상의 숫자가 나옵니다.
```

**안 되면**
- `command not found` -> nodejs.org 에서 LTS 를 받습니다. 설치 후 VS Code 를 껐다 켭니다.
- 숫자가 `node_min` 보다 작다 -> 같은 곳에서 LTS 를 다시 받습니다.
  버전이 낮으면 설치가 도중에 멈추는데 화면에는 다른 오류로 나옵니다.

## W1-B. 패키지를 설치합니다 (`package.json` 이 있을 때)

터미널 맨 앞이 이 폴더인지 먼저 봅니다. 아니면 `cd booking-hub` 로 옮깁니다.

```
npm install
```

1-2 분 걸립니다. 끝나면 `node_modules` 폴더가 생깁니다.

**이렇게 보이면 성공**
```
added 숫자 packages 로 끝나고 터미널이 다시 입력을 받습니다.
```

**안 되면**
- `ERESOLVE` -> `npm install --legacy-peer-deps` 로 한 번 더 합니다.
- `EACCES` 나 권한 오류 -> 폴더가 바탕화면에 있는지 봅니다. 다운로드 폴더 안에서 압축이
  덜 풀린 상태면 이 오류가 납니다.
- 중간에 멈춘 것 같다 -> `node_modules` 를 지우고 다시 `npm install` 합니다.

## W2. 표를 만듭니다 (Supabase 를 쓰는 회차)

`_table.sql` 을 통째로 복사해 supabase.com 의 `SQL Editor` 에 붙여넣고 `Run`.

**이렇게 보이면 성공**
```
Success. No rows returned
```

**안 되면**
- 파일 끝의 `create policy` 문을 빼먹으면 저장이 전부 거부됩니다. 통째로 다시 붙여넣습니다.

## W3. 열쇠를 넣습니다

`.env.example` 을 복사해 `.env` 를 만들고 값을 채웁니다.
`=` 앞뒤에 빈칸을 넣지 않습니다. 따옴표도 넣지 않습니다.

## W4. 확인합니다

```
node check.mjs
```

**안 되면**
- `표 없음` -> W2 로. `키 없음` -> W3 으로.

## W5. 로컬에서 화면을 봅니다

`package.json` 의 `scripts` 에 `dev` 가 있으면 이것입니다.

```
npm run dev
```

없으면 이것입니다.

```
npx serve . -l 5500
```

터미널에 나온 `http://localhost:...` 주소를 브라우저에서 엽니다.
`npm run dev` 는 파일을 고치면 화면이 바로 바뀝니다. 끄는 것은 `Ctrl + C`.

**안 되면**
- `Port is already in use` -> 이미 떠 있는 것입니다. 터미널에 나온 다른 포트 번호를 씁니다.
- 화면이 하얗다 -> 브라우저에서 `F12` 를 눌러 `Console` 탭의 빨간 줄을 읽습니다.

## W6. GitHub 에 올립니다 (`repo` 가 있는 회차)

한 줄씩 보냅니다. `.gitignore` 에 `.env` 가 있는지 먼저 확인합니다.

```
git init
```

```
git add -A
```

```
git commit -m "첫 커밋"
```

```
gh repo create 내저장소이름 --public --source . --push
```

**안 되면**
- `gh: command not found` -> github.com 우상단 `+` 에서 `New repository` 로 만들고,
  화면에 나오는 `git remote add origin ...` 두 줄을 그대로 실행합니다.
- `Permission denied (publickey)` -> `gh auth login` 을 하거나 주소를 `https://` 로 바꿉니다.

## W7. 배포합니다

```
npx vercel login
```

```
npx vercel --prod --yes
```

처음이면 브라우저에서 로그인 창이 열립니다. 프로젝트 이름을 물으면 엔터.

## W8. 짧은 주소를 찾습니다

```
npx vercel ls
```

`--prod` 가 출력한 **긴 주소는 보호가 걸려 있어 열리지 않습니다.**
`이름.vercel.app` 형태의 **짧은 주소**를 씁니다.

## W9. 환경변수를 넣고 다시 배포합니다

배포 주소 뒤에 `/api/config` 를 붙여 열어봅니다. `환경변수 미설정` 이면
vercel.com 에서 내 프로젝트 `Settings` -> `Environment Variables` 에
`.env` 의 이름과 값을 그대로 넣고 `Production` 체크 후 `Save`.

```
npx vercel --prod --yes
```

환경변수는 **다시 배포해야 반영됩니다.**

## W10. 폰으로 확인합니다

짧은 주소를 카카오톡 "나에게 보내기" 로 보내 폰에서 링크를 누릅니다.
손으로 옮겨 적다 틀리는 사례가 많습니다.

---

막히면 같은 폴더의 `_troubleshoot.md` - 웹은 갈래 D / D2(GitHub) / D3(Supabase) 입니다.
