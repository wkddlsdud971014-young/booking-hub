# D32 S3 - 상태 변경 + 위치 조회 + 집계 (13:00-13:50)

## 지난 시간 복습
### S2에서 만든 것
BookingTable(예약 목록)과 BookingForm(예약 추가)이 작동하는 상태입니다. 폼에 입력하면 표에 바로 나타납니다. S3에서는 나머지 세 기능을 만듭니다.

## 오늘의 학습 목표
### 이 세션 주제: StatusToggle + MapLink + StatCards
1. 예약 상태를 pending에서 confirmed로 토글할 수 있다 (Intent 3: 상태 변경)
2. 주소를 Google Maps 링크로 열 수 있다 (Intent 4: 위치 조회)
3. 오늘/이번 주 예약 건수를 숫자 카드로 볼 수 있다 (Intent 5: 집계)

이 세션이 끝나면 **표에서 상태 토글이 작동하고, 주소 클릭하면 지도가 열리고, 상단에 숫자 카드가 보여야** S4로 넘어갑니다.

---

# Part A. 상태 변경 (StatusToggle)

## 1. 토글 버튼 만들기

### 1.1 핵심
예약 행마다 상태를 바꾸는 버튼을 넣습니다. `pending`(대기)이면 노란 배지, `confirmed`(확정)이면 초록 배지. 배지를 클릭하면 상태가 반대로 바뀌고 Supabase에 즉시 저장됩니다.

### 1.2 처음이면

[프롬프트]
BookingTable.tsx를 수정해줘.
- 각 행의 status 칼럼에 클릭 가능한 배지 버튼을 넣어줘
- pending이면 노란 배경(bg-yellow-100 text-yellow-800), confirmed이면 초록 배경(bg-green-100 text-green-800)
- 배지 클릭하면 supabase.from('bookings').update({status: 반대값}).eq('id', 해당행id) 실행
- 성공하면 목록 다시 조회
[프롬프트 끝]

브라우저에서 아까 추가한 예약의 상태 배지를 클릭합니다. 노란색이 초록색으로 바뀌면 성공.

**완료 기준**: pending 배지 클릭 -> confirmed로 바뀐다. 다시 클릭 -> pending으로 돌아간다. Supabase 대시보드에서도 status 값이 바뀐다.

### 1.3 안 되면
증상 1: 배지 클릭해도 안 바뀝니다
1. 콘솔에 `403`이면 RLS에 update 정책 추가 필요.
2. Supabase 대시보드 -> Policies -> `Enable update access for all users` 추가.

증상 2: 색상은 바뀌는데 새로고침하면 원래로 돌아갑니다
1. Supabase에 실제로 저장이 안 된 상태. `update` 호출의 `.eq('id', ...)` 부분 확인.
2. id가 `undefined`면 데이터 가져올 때 id 칼럼을 포함했는지 확인.

---

# Part B. 위치 조회 (MapLink)

## 1. 주소를 지도 링크로

### 1.1 핵심
예약의 `address` 칼럼 값을 Google Maps 링크로 만들어서 클릭하면 지도가 열리게 합니다. 서버 호출 없이 프론트에서 주소 문자열을 URL에 넣기만 하면 됩니다. 내일(목) 폰에서 열면 Maps 앱이 바로 뜹니다.

### 1.2 처음이면

[프롬프트]
BookingTable.tsx에 지도 링크를 추가해줘.
- address 칼럼 값을 표시할 때 Google Maps 링크로 감싸줘
- 링크 주소: https://maps.google.com/?q={address를 encodeURIComponent로 인코딩}
- 링크 텍스트는 주소 그대로, 파란색 밑줄
- target="_blank"로 새 탭에서 열기
- 주소가 비어있으면 "-" 표시
[프롬프트 끝]

표에서 주소를 클릭하면 새 탭에 Google Maps가 열리고 해당 위치가 표시됩니다.

**완료 기준**: 표의 주소 칼럼이 파란 링크가 됐고, 클릭하면 Google Maps에서 해당 위치가 열린다.

### 1.3 안 되면
증상 1: 링크 클릭해도 지도에 아무 핀이 안 찍힙니다
1. 주소가 "강남역" 같은 한국어면 정상 작동합니다. "abc123" 같은 의미 없는 문자열이면 구글이 위치를 못 찾아요.
2. 테스트 데이터의 address를 실제 주소로 수정해보세요 ("서울 강남구 강남대로 396").

---

# Part C. 집계 카드 (StatCards)

## 1. 숫자 카드 3개

### 1.1 핵심
대시보드 상단에 카드 3개를 놓습니다. **오늘 예약 수**, **확정률**, **이번 주 총 건수**. 별도 집계 테이블 없이 bookings 데이터에서 매번 계산합니다.

### 1.2 처음이면

[프롬프트]
src/components/StatCards.tsx 파일을 만들어줘.
- supabase에서 bookings 전체 데이터를 가져와서 계산:
  1. 오늘 예약 수: date가 오늘인 행 개수
  2. 확정률: confirmed 행 / 전체 행 * 100 (소수점 1자리)
  3. 이번 주 총 건수: 이번 주 월-금 범위의 행 개수
- 카드 3개를 가로로 나란히 배치 (flex gap-4)
- 각 카드: 흰 배경, 그림자, 큰 숫자 + 작은 라벨
- 데이터 없으면 전부 0 표시
[프롬프트 끝]

App.tsx에서 StatCards를 최상단에 배치합니다.

[프롬프트]
App.tsx에 StatCards를 추가해줘.
- 제목("예약 관리 허브") 바로 아래, BookingForm 위에 배치
- BookingForm의 onSuccess 때 StatCards도 갱신되도록 refreshKey 공유
[프롬프트 끝]

**완료 기준**: 상단에 숫자 카드 3개가 보이고, 예약을 추가하면 숫자가 바뀐다.

### 1.3 안 되면
증상 1: 숫자가 전부 0입니다
1. bookings에 데이터가 있는지 확인. 아까 추가한 예약의 date가 오늘이 아니면 "오늘 예약 수"는 0이 맞습니다.
2. 테스트로 date를 오늘 날짜로 바꿔서 확인.

증상 2: 카드가 세로로 쌓입니다 (가로 아님)
1. StatCards의 부모에 `flex gap-4`가 있는지 확인.
2. 화면 폭이 좁으면 `flex-wrap`으로 줄바꿈될 수 있음. 정상.

---

# Part D. 오늘 세션 정리

## 이 세션에서 만든 것
- 상태 토글 (Intent 3: pending <-> confirmed)
- 지도 링크 (Intent 4: 주소 클릭 -> Google Maps)
- 집계 카드 (Intent 5: 오늘 예약 수/확정률/이번 주 총)

## 완료 체크리스트
- [ ] 상태 배지 클릭하면 pending/confirmed 토글
- [ ] 주소 클릭하면 Google Maps 열림
- [ ] 상단 숫자 카드 3개에 실제 숫자 표시
- [ ] 예약 추가/상태 변경하면 카드 숫자도 갱신

## 다음 세션으로
S4에서 이 5개 기능을 탭 네비게이션으로 묶어서 **허브앱**을 완성합니다. 지금은 한 화면에 다 쌓여있지만, S4 끝나면 탭을 눌러서 화면을 전환하는 앱이 됩니다. 서버 끄지 마세요.
