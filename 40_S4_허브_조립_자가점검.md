# D32 S4 - 허브 조립 + 자가점검 (14:00-14:50)

## 지난 시간 복습
### S1-S3에서 만든 것
S1에서 프로젝트를 만들고 Supabase에 연결했습니다. S2에서 목록 조회(BookingTable)와 예약 추가(BookingForm)를 만들었습니다. S3에서 상태 토글, 지도 링크, 집계 카드를 추가했습니다. 지금은 5개 기능이 한 화면에 다 쌓여 있는 상태입니다. S4에서 탭 네비게이션으로 묶어서 **허브앱**으로 완성합니다.

## 오늘의 학습 목표
### 이 세션 주제: 5탭 App.tsx + 자가점검 체크리스트
1. 5개 기능을 탭 네비게이션으로 분리할 수 있다
2. 탭을 누르면 해당 화면만 보이게 전환할 수 있다
3. 자가점검 체크리스트로 5개 기능 전부 작동을 확인할 수 있다

이 세션이 끝나면 **탭 5개가 있는 허브앱**이 완성됩니다. 내일(목) 이 앱을 Vercel에 올려서 폰에서 열어봅니다.

---

# Part A. 탭 네비게이션 조립

## 1. App.tsx를 탭 구조로 변경

### 1.1 핵심
지금 App.tsx에 컴포넌트가 전부 나열돼 있습니다. 이걸 탭 5개로 나눕니다. 하단에 탭 바를 놓고, 탭을 누르면 해당 화면만 보입니다. 라이브러리 없이 useState 하나로 만듭니다.

### 1.2 처음이면

[프롬프트]
App.tsx를 탭 네비게이션 구조로 바꿔줘.
- 탭 5개: 대시보드 / 예약목록 / 예약추가 / 상태관리 / 위치확인
- 하단에 탭 바 (fixed bottom, 배경 흰색, 그림자)
- 각 탭 버튼: 탭 이름 텍스트, 선택된 탭은 파란색, 나머지 회색
- useState로 activeTab 관리
- 탭별 화면 구성:
  - 대시보드 = StatCards
  - 예약목록 = BookingTable
  - 예약추가 = BookingForm (성공 시 예약목록 탭으로 자동 이동)
  - 상태관리 = BookingTable인데 상태 토글 기능만 강조
  - 위치확인 = BookingTable인데 주소/지도 링크만 강조
- 상단에 "예약 관리 허브" 제목 유지
- 하단 탭 바 높이만큼 본문에 padding-bottom 추가 (내용이 가려지지 않도록)
[프롬프트 끝]

브라우저에서 탭을 하나씩 눌러봅니다. 각 탭이 전환되면 성공.

**완료 기준**: 하단 탭 5개가 보이고, 누르면 화면이 전환된다.

### 1.3 안 되면
증상 1: 탭 바가 보이지 않습니다
1. `fixed bottom-0`이 적용됐는지 확인. `position: fixed`와 `bottom: 0`이 있어야 합니다.
2. `z-index`가 충분히 높은지 확인 (z-50 정도).

증상 2: 탭을 눌러도 화면이 안 바뀝니다
1. activeTab 상태와 탭 버튼의 onClick이 연결됐는지 확인.
2. 각 탭 화면의 조건부 렌더링이 `activeTab === '대시보드'` 같은 형태인지 확인.

증상 3: 하단 컨텐츠가 탭 바에 가려집니다
1. 본문 컨테이너에 `pb-20` 정도의 padding-bottom을 추가.

---

# Part B. 자가점검 체크리스트

## 1. 5개 기능 전수 확인

아래 체크리스트를 하나씩 실행합니다. 전부 통과해야 D32 완료입니다.

### Intent 1. 목록 조회
- [ ] "예약목록" 탭을 누르면 표가 보인다
- [ ] Supabase에 데이터가 있으면 표에 행이 나타난다
- [ ] 데이터가 없으면 "예약이 없습니다"가 보인다

### Intent 2. 예약 추가
- [ ] "예약추가" 탭에서 폼 5칸을 채우고 "예약하기" 클릭
- [ ] 성공하면 "예약목록" 탭으로 자동 이동하고 방금 입력한 예약이 보인다
- [ ] Supabase 대시보드에서 via 칼럼이 `form`인 것을 확인

### Intent 3. 상태 변경
- [ ] "상태관리" 탭에서 pending 배지를 클릭하면 confirmed로 바뀐다
- [ ] 다시 클릭하면 pending으로 돌아간다
- [ ] Supabase 대시보드에서 status 값이 실제로 바뀐다

### Intent 4. 위치 확인
- [ ] "위치확인" 탭에서 주소 링크를 클릭하면 Google Maps가 새 탭에 열린다
- [ ] 지도에 해당 위치가 표시된다
- [ ] 주소가 없는 행은 "-"으로 표시된다

### Intent 5. 집계
- [ ] "대시보드" 탭에서 숫자 카드 3개가 보인다
- [ ] 예약을 추가하면 숫자가 올라간다
- [ ] 상태를 변경하면 확정률이 바뀐다

### 1.1 안 되면
하나라도 통과 못 하면 해당 S 세션의 "안 되면" 절을 다시 봅니다.
- Intent 1, 2 문제 -> S2 안 되면 절
- Intent 3, 4, 5 문제 -> S3 안 되면 절
- 탭 전환 문제 -> 위 Part A 안 되면 절

---

# Part C. 오늘 세션 정리

## D32 전체 산출물
오늘 하루에 만든 것:
- React + Vite + TypeScript + Tailwind 프로젝트 `booking-hub`
- Supabase `bookings` 테이블 (칼럼 9개)
- 5개 컴포넌트: BookingTable, BookingForm, StatusToggle(내장), MapLink(내장), StatCards
- 탭 네비게이션으로 묶은 허브앱

## 내일 미리보기 (D33 목요일)
내일은 이 앱을 **Vercel에 올려서 폰으로 엽니다.** 코드 변경 없이 배포만 합니다. PC에서 만든 것이 폰에서 그대로 돌아가는 것을 확인합니다. GitHub에 코드를 올리고, Vercel을 연결하면 URL이 나옵니다. 그 URL을 폰 브라우저에 붙여넣으면 방금 만든 허브앱이 뜹니다.

## Git 커밋
오늘 작업한 코드를 저장합니다. 터미널에서:
```bash
git add .
git commit -m "D32: booking hub React SPA with 5 tabs"
```
