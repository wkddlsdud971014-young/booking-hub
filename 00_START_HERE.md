# Day 32 - 예약 관리 허브앱을 만드는 날

2026년 9월 2일 수요일

## 오늘 하루 세 줄

- 뼈대 프로젝트에서 시작해서 React 앱 하나를 끝까지 만듭니다. Supabase 데이터베이스에 예약을 넣고, 읽고, 상태를 바꾸고, 지도를 열고, 숫자를 세는 앱입니다.
- 코드를 직접 치지 않습니다. VS Code(또는 Cursor)에 프롬프트를 붙여넣으면 AI가 코드를 만들고, 본인은 결과를 확인합니다.
- 마지막에 탭 5개짜리 허브앱이 완성됩니다. 내일 이 앱을 폰에서 열어봅니다.

## 0단계 - 받은 압축을 제자리에 풉니다

이 파일이 VS Code 왼쪽 목록에 보인다면 0단계는 끝난 것입니다. 1단계로 갑니다.

### dev 폴더를 만듭니다

실습 폴더는 `dev` 폴더 안에 둡니다. 바탕화면이나 다운로드 폴더에 두면 경로에 한글이나 빈칸이 섞여 설치가 중간에 멈춥니다.

**Windows** - `C` 드라이브에 `dev` 폴더 하나를 만듭니다. 최종 위치는 `C:\dev\booking-hub` 입니다.

**Mac** - 터미널에 이 한 줄이면 만들어지고 창까지 열립니다.

```bash
mkdir -p ~/dev && open ~/dev
```

Finder로 찾아가려면 `매킨토시 HD > 사용자 > 내 계정 이름` 입니다. 왼쪽에 매킨토시 HD가 안 보이면 위 명령이 빠릅니다.

### 압축을 옮긴 뒤 풉니다

받은 `booking-hub.zip` 을 **`dev` 폴더로 옮긴 다음 거기서** 풉니다. 다운로드 폴더에서 풀고 나중에 옮기면 경로가 두 군데로 갈립니다.

`dev` 폴더 안에 `booking-hub` 폴더가 생깁니다.

### VS Code로 폴더를 엽니다

메뉴 `File > Open Folder` 로 **`booking-hub` 폴더 자체**를 엽니다. 폴더 안의 파일이 아니라 폴더입니다.

**이렇게 보이면 성공**

```
왼쪽 목록에 00_START_HERE.md 와 package.json 이 보입니다.
창 위쪽 제목이 booking-hub 입니다.
```

**안 되면**

| 증상 | 어떻게 |
|---|---|
| 파일 하나만 열렸다 | 폴더를 안 연 것입니다. `File > Open Folder` 를 다시 |
| 왼쪽에 `booking-hub` 가 또 있다 | 폴더가 두 겹입니다. 안쪽 것을 엽니다 |
| 폴더 이름이 `booking-hub 2` | 압축을 두 번 풀었습니다. 나머지를 지우고 하나만 남깁니다 |
| `.env.example` 이 안 보인다 | 탐색기와 Finder는 점 파일을 숨깁니다. VS Code 왼쪽 목록에는 보입니다 |

### 한글이 들어가면 안 되는 곳 셋

| 어디 | 확인 |
|---|---|
| 폴더 이름 | `dev`, `booking-hub` - 영문 그대로 씁니다 |
| Windows 사용자 계정 이름 | `C:\Users\` 안의 내 폴더가 한글이면 **강사 호명** |
| 컴퓨터 이름 | 한글이면 **강사 호명** |

뒤 둘은 옮겨도 안 풀립니다. 혼자 고치려 하지 않습니다.

## 1단계 - 뼈대를 켭니다

AI 채팅창에 **"실행해줘"** 라고 씁니다.

같은 폴더의 `AGENTS.md` 를 AI가 읽고 설치와 실행을 대신 합니다. 설치는 1-2분 걸립니다. 그동안 아래를 읽어둡니다.

브라우저에 파란 큰 글자 **연결 테스트**가 뜨면 준비 끝입니다.

터미널을 직접 쓰고 싶으면 같은 폴더의 `명령어.md` 에 복사할 줄이 순서대로 있습니다.

## 시작 전 확인 - 4가지

| 도구 | 확인 방법 | 안 되면 |
|---|---|---|
| Node.js | 터미널에 `node -v` 입력, 버전 숫자가 나온다 | 강사 호명 |
| VS Code 또는 Cursor | 열려 있고 터미널 패널이 보인다 | `Ctrl+`` 으로 터미널 열기 |
| 브라우저 | 새 탭을 열 수 있다 | 아무 브라우저나 하나 |
| Supabase | supabase.com에 로그인된 상태 | 이전 수업에서 만든 계정으로 로그인 |

4가지 다 확인되고 화면에 "연결 테스트"가 보이면 S1 시작.

## 하루 흐름표

| 세션 | 시각 | 무엇을 하나 | 끝나면 남는 것 |
|---|---|---|---|
| S1 | 10:00-10:50 | 빈 프로젝트를 만들고 Supabase에 연결 | 브라우저에 "연결 성공" + 콘솔에 빈 배열 |
| S2 | 11:00-11:50 | 예약 목록 표 + 예약 추가 폼 만들기 | 폼에 입력하면 표에 바로 나타남 |
| 점심 | 11:50-13:00 | - | - |
| S3 | 13:00-13:50 | 상태 토글 + 지도 링크 + 숫자 카드 | 표에서 상태 바꾸기, 주소 클릭하면 지도 열림 |
| S4 | 14:00-14:50 | 탭 5개로 묶기 + 자가점검 | 하단 탭 바가 있는 완성 앱 |

## 오늘의 도구

- **프롬프트 13개**: 가이드 파일 안에 [프롬프트] 블록으로 표시되어 있습니다. 그 안의 텍스트를 VS Code 채팅창에 붙여넣으면 됩니다.
- **Supabase 대시보드**: S1에서 테이블을 만들 때 한 번, S2에서 데이터 확인할 때 한 번 씁니다.
- **직접 하는 일 3가지**: (1) supabase.com 로그인 (2) `_table.sql` 을 SQL Editor에 붙여넣고 Run (3) Settings -> API에서 키 두 개를 복사해 `.env` 에 붙여넣기. 나머지 설치·실행·코드 작성은 AI가 합니다.

**프롬프트 1-2는 뼈대에 이미 적용되어 있습니다.** 프로젝트 생성(프롬프트 1)과 Tailwind 설치(프롬프트 2)의 결과가 `booking-hub` 폴더입니다. 가이드에서 그 두 개를 만나면 무엇이 적용된 것인지 확인만 하고 프롬프트 3으로 넘어갑니다. 나중에 자기 프로젝트를 처음부터 만들 때 그 두 개를 그대로 씁니다.

## 완료 기준표

**S1 [4/4]**

| 항목 | 확인 방법 |
|---|---|
| 뼈대가 돈다 | 브라우저에 앱이 뜬다 |
| Tailwind가 작동한다 | 파란 큰 글자 "연결 테스트"가 보인다 |
| Supabase 테이블이 있다 | 대시보드 Table Editor에 `bookings` 테이블과 칼럼 9개가 보인다 |
| 연결이 된다 | `node check.mjs` 가 `키 OK` 와 `표 OK` 두 줄을 낸다 |

**S2 [3/3]**

| 항목 | 확인 방법 |
|---|---|
| 목록이 보인다 | 테스트 데이터가 표에 나타난다 |
| 폼이 작동한다 | 폼에 입력하고 버튼 누르면 표에 새 행이 추가된다 |
| via가 'form'이다 | Supabase 대시보드에서 via 칼럼이 'form'으로 들어가 있다 |

**S3 [3/3]**

| 항목 | 확인 방법 |
|---|---|
| 상태 토글 | 표에서 pending/confirmed 배지를 클릭하면 상태가 바뀐다 |
| 지도 링크 | 주소를 클릭하면 Google Maps가 열린다 |
| 집계 카드 | 상단에 오늘 예약 건수와 전체 건수 숫자가 보인다 |

**S4 [2/2]**

| 항목 | 확인 방법 |
|---|---|
| 탭 전환 | 하단 탭 5개를 누르면 각각 다른 화면이 나온다 |
| 자가점검 통과 | 체크리스트 항목을 하나씩 확인해서 전부 작동한다 |

**부분 통과선**: S2까지만 끝나도 됩니다. 목록 + 폼이 작동하면 오늘 몫입니다.

## 파일 안내

전부 `booking-hub` 폴더 안에 있습니다. VS Code 한 창에서 열어둔 채로 씁니다.

| 파일 | 언제 |
|---|---|
| `00_START_HERE.md` | 이 파일. 지금 읽고 있습니다 |
| `10_S1_프로젝트_생성_Supabase_연결.md` | S1 시작할 때 |
| `20_S2_예약_목록과_추가.md` | S2 시작할 때 |
| `30_S3_상태_위치_집계.md` | S3 시작할 때 |
| `40_S4_허브_조립_자가점검.md` | S4 시작할 때 |
| `AGENTS.md` | AI가 읽습니다. 직접 열지 않아도 됩니다 |
| `명령어.md` | 터미널을 직접 쓸 때 복사할 줄 |
| `_table.sql` | S1에서 Supabase SQL Editor에 붙여넣습니다 |
| `.env.example` | S1에서 `.env` 를 만들 때 본을 뜹니다 |
| `check.mjs` | 키와 표가 맞는지 확인합니다. `node check.mjs` |
| `_troubleshoot.md` | 막혔을 때 증상으로 찾습니다 |
| `_env.yaml` | 이 회차의 값. 고치지 않습니다 |

프롬프트는 가이드 파일 안에 있습니다. VS Code에서 가이드를 열어두고 거기서 복사해 AI 채팅창에 붙여넣습니다. 창을 오갈 일이 없습니다.

## 따라잡기 지점

- **S2부터 합류**: S1이 끝난 프로젝트 파일을 받아서 시작합니다. `npm install` -> `npm run dev`로 바로 S2 시작.
- **S3부터 합류**: S2까지 끝난 프로젝트 파일을 받아서 시작합니다. 폼과 표가 이미 작동하는 상태.
- **S4부터 합류**: S3까지 끝난 프로젝트를 받으면 탭 조립만 합니다.

## bookings 테이블 칼럼

표를 만드는 SQL은 `booking-hub/_table.sql` 에 있습니다. 통째로 복사해 Supabase SQL Editor에 붙여넣고 Run 하면 아래 칼럼이 생깁니다. **파일 끝의 `create policy` 세 개까지 한 번에 붙여넣습니다.** 표만 만들고 멈추면 앱에서 아무것도 안 보입니다.

| 칼럼 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| id | int8 | (자동) | 행 번호, 자동 생성 |
| customer | text | (없음) | 고객사 이름 |
| service | text | (없음) | 서비스(컨설팅 유형) |
| date | text | (없음) | 날짜 |
| time | text | (없음) | 시간 |
| address | text | (없음) | 고객사 주소 |
| status | text | 'pending' | 미팅 상태 (pending/confirmed) |
| via | text | 'form' | 등록 경로 (form=직접 / agent=AI비서) |
| created_at | timestamptz | now() | 등록 시각 |
