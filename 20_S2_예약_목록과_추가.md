# D32 S2 - 예약 목록 조회 + 예약 추가 (11:00-11:50)

## 지난 시간 복습
### S1에서 만든 것
빈 React 앱이 Supabase에 연결된 상태입니다. 콘솔에 빈 배열 `[]`이 찍히는 것까지 확인했습니다. S2에서는 이 연결 위에 **예약 목록을 표로 보여주는 컴포넌트**와 **예약을 추가하는 폼 컴포넌트** 두 개를 만듭니다.

## 오늘의 학습 목표
### 이 세션 주제: BookingTable + BookingForm 컴포넌트
1. Supabase에서 데이터를 읽어서 화면에 표로 보여줄 수 있다 (Intent 1: 목록 조회)
2. 폼에 입력한 데이터를 Supabase 테이블에 저장할 수 있다 (Intent 2: 예약 추가)
3. 폼으로 추가한 예약이 표에 바로 나타나는 것을 확인할 수 있다

이 세션이 끝나면 **폼에 예약을 입력하면 아래 표에 바로 추가되어야** S3로 넘어갑니다.

---

# Part A. 예약 목록 컴포넌트 (BookingTable)

## 1. 목록 조회 만들기

### 1.1 핵심
`BookingTable`은 Supabase `bookings` 테이블의 데이터를 읽어서 HTML 표로 보여주는 컴포넌트입니다. 페이지가 열리면 자동으로 데이터를 가져오고, 고객사/서비스/날짜/시간/상태를 한 행씩 보여줍니다.

### 1.2 처음이면
Cursor에 아래 프롬프트를 붙여넣습니다.

[프롬프트]
src/components/BookingTable.tsx 파일을 만들어줘.
- supabase에서 bookings 테이블의 전체 행을 가져온다
- useEffect + useState로 페이지 로드 시 자동 조회
- 결과를 HTML table로 렌더. 칼럼: 고객사(customer), 서비스(service), 날짜(date), 시간(time), 상태(status)
- Tailwind로 테이블 스타일링 (border, padding, hover 색상)
- 데이터가 없으면 "예약이 없습니다" 표시
- 조회 중이면 "로딩 중..." 표시
[프롬프트 끝]

만들어진 파일을 `App.tsx`에서 import해서 화면에 보여줍니다.

[프롬프트]
App.tsx에서 BookingTable 컴포넌트를 import해서 보여줘.
- "연결 테스트" 대신 BookingTable을 렌더
- 상단에 "예약 관리" 제목 추가
[프롬프트 끝]

브라우저를 새로고침하면 "예약이 없습니다"가 보입니다. 정상입니다 - 아직 데이터가 없으니까요.

테스트 데이터를 하나 넣어봅니다. Supabase 대시보드 -> Table Editor -> bookings -> **Insert row** 클릭. customer에 "테스트", service에 "커트", date에 "2026-09-03", time에 "14:00"을 입력하고 Save. 브라우저를 새로고침하면 표에 한 행이 보입니다.

**완료 기준**: Supabase에 넣은 테스트 데이터가 브라우저 표에 나타난다.

### 1.3 안 되면
증상 1: "예약이 없습니다"가 계속 보입니다 (데이터를 넣었는데도)
1. Supabase 대시보드에서 행이 실제로 저장됐는지 확인.
2. 브라우저 콘솔에 에러가 없는지 확인.
3. RLS(Row Level Security) 때문에 읽기가 차단될 수 있음. Supabase 대시보드 -> Authentication -> Policies -> bookings 테이블에 `Enable read access for all users` 정책 추가.

증상 2: 콘솔에 `relation "bookings" does not exist`
1. 테이블 이름이 정확히 `bookings`인지 확인 (대소문자 주의).
2. S1에서 만든 테이블과 다른 프로젝트에 접속하고 있을 수 있음. `.env`의 URL 재확인.

---

# Part B. 예약 추가 컴포넌트 (BookingForm)

## 1. 예약 폼 만들기

### 1.1 핵심
`BookingForm`은 고객사/서비스/날짜/시간/주소 5칸을 입력받아 Supabase에 새 행을 추가하는 컴포넌트입니다. 추가가 성공하면 표가 자동으로 갱신되어 방금 입력한 예약이 바로 보여야 합니다.

### 1.2 처음이면

[프롬프트]
src/components/BookingForm.tsx 파일을 만들어줘.
- 입력 칸 5개: 고객사(customer), 서비스(service), 날짜(date), 시간(time), 주소(address)
- 날짜는 input type="date", 시간은 input type="time" 사용
- "예약하기" 버튼 클릭하면 supabase.from('bookings').insert({...}) 실행
- via 칼럼에는 'form'이 자동으로 들어감 (Supabase 테이블 기본값)
- 성공하면 폼 초기화 + onSuccess 콜백 호출 (부모가 표를 갱신하도록)
- 실패하면 에러 메시지 표시
- Tailwind로 폼 스타일링 (입력 칸 테두리, 버튼 파란색)
[프롬프트 끝]

App.tsx에서 BookingForm과 BookingTable을 함께 보여줍니다.

[프롬프트]
App.tsx를 수정해줘.
- BookingForm을 위에, BookingTable을 아래에 배치
- BookingForm의 onSuccess가 호출되면 BookingTable이 다시 조회하도록 연결
  (방법: useState로 refreshKey를 관리하고 onSuccess 때 +1, BookingTable의 useEffect dependency에 넣기)
- 상단 제목: "예약 관리 허브"
[프롬프트 끝]

브라우저에서 폼에 데이터를 입력하고 "예약하기"를 클릭합니다. 아래 표에 방금 입력한 예약이 추가되면 성공.

**완료 기준**: 폼에 예약을 입력하고 버튼을 누르면 아래 표에 새 행이 나타난다. Supabase 대시보드에서도 행이 추가된 것이 보인다.

### 1.3 안 되면
증상 1: "예약하기" 버튼을 눌러도 아무 반응이 없습니다
1. 브라우저 콘솔 확인. `403 Forbidden`이면 RLS 정책 문제.
2. Supabase 대시보드 -> Authentication -> Policies -> bookings에 `Enable insert access for all users` 정책 추가.

증상 2: 예약은 추가되는데 표가 자동 갱신되지 않습니다
1. onSuccess 콜백이 연결 안 된 상태. App.tsx에서 refreshKey 로직 확인.
2. 수동 새로고침으로 표가 갱신되면 콜백 연결만 고치면 됩니다.

증상 3: `null value in column "customer" violates not-null constraint`
1. 폼 칸이 비어 있는 상태에서 제출한 경우. 필수 칸 validation 추가 필요.
2. 빈 칸 있으면 버튼 비활성화하거나 에러 메시지 표시.

---

# Part C. 오늘 세션 정리

## 이 세션에서 만든 것
- `BookingTable` 컴포넌트 (Intent 1: 예약 목록 조회)
- `BookingForm` 컴포넌트 (Intent 2: 예약 추가)
- 폼 제출 -> 표 자동 갱신 연결

## 완료 체크리스트
- [ ] 폼에 예약을 입력하면 표에 바로 나타난다
- [ ] Supabase 대시보드에서도 행이 추가된다
- [ ] via 칼럼에 'form'이 들어가 있다

## 다음 세션으로
S3에서는 나머지 세 가지 기능을 만듭니다. **상태 변경**(pending을 confirmed로 토글), **위치 확인**(주소를 지도 링크로), **집계**(오늘 예약 건수). S2 끝난 상태에서 서버 끄지 마세요 - S3에서 이어 작업합니다.
